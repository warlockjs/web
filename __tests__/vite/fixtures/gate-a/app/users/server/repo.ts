// A `server/` folder that is NOT under any `web/` folder — the rule follows
// the app source, not the client-surface convention.
export function usersServerRepo() {
  return "users-server-repo-value";
}
